apiVersion: v1
kind: Service
metadata:
  name: jenkinswebapi-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: jenkinswebapi
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
