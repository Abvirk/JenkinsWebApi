apiVersion: v1
kind: Service
metadata:
  name: jenkinswebapi-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: jenkinswebapi
  ports:
  - name: http
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30205
